const zone=document.querySelector('.stacked-zone');const track=document.querySelector('.stacked-track');const cards=[...document.querySelectorAll('.merch-card')];function updateParallax(){if(!zone||!track||window.innerWidth<=900)return;const rect=zone.getBoundingClientRect();const scrollable=zone.offsetHeight-window.innerHeight;const progress=Math.min(1,Math.max(0,-rect.top/scrollable));const maxX=Math.max(0,track.scrollWidth-window.innerWidth);track.style.transform=`translate3d(${-progress*maxX}px,0,0)`;cards.forEach((card,i)=>{const center=card.getBoundingClientRect().left+card.offsetWidth/2;const delta=(center-window.innerWidth/2)/window.innerWidth;const stage=card.querySelector('.product-stage,.pick-cluster');if(stage)stage.style.transform=`translate3d(${delta*-34}px,${Math.abs(delta)*10}px,0) rotate(${delta*-1.5}deg)`;});}let ticking=false;window.addEventListener('scroll',()=>{if(ticking)return;ticking=true;requestAnimationFrame(()=>{updateParallax();ticking=false;});},{passive:true});window.addEventListener('resize',updateParallax);updateParallax();const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('seen')}),{threshold:.18});cards.forEach(c=>observer.observe(c));